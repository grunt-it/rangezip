{
  "name": "rangezip-sample-builder",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "TEMPORARY Cloudflare Worker that generates + uploads the 10/30 GB rangezip ZIP64 samples entirely server-side (DO -> R2 multipart). Undeploy with `wrangler delete` after the uploads; the script stays in the repo for reproducibility.",
  "scripts": {
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "delete": "wrangler delete"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260522.1",
    "typescript": "^6.0.3",
    "wrangler": "^4.94.0"
  }
}
